var mass=function(height, weight) {
    return height / weight 
    };
    var height = parseInt(prompt("what is your height?"));
    var weight = parseInt(prompt("what is your weight?"));
    alert(mass(height, weight));